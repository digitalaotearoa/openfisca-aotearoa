version: "3"

services:
  openfisca:
    build:
      context: .
    restart: always
    labels:
      lagoon.type: python
    networks:
      - web
    volumes:
      - .:/openfisca

networks:
  web:
    external: true
